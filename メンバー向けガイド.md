# Offi-Stretch リストアップツール メンバー向けガイド

---

## 全体構成

| 役割 | 場所 | 説明 |
|---|---|---|
| リストアップ（常時自動） | 専用PC（常時起動） | 24時間自動で企業を探し続ける |
| 架電記録・商談記録・手動リストアップ | Webツール（ブラウザ） | どこからでもアクセス可能 |
| CRM登録先 | HubSpot | 両方から登録される。重複は自動除外 |

---

## A. Webツールの使い方（全員共通）

### アクセス

ブラウザで下記URLを開く：

```
https://list-tool.onrender.com/
```

### タブ一覧

| タブ | 用途 |
|---|---|
| 📞 架電記録 | テレアポの結果を入力する |
| 📋 履歴 | 過去の架電履歴を確認する |
| 📊 分析 | ランク別アポ率・断り理由などを確認する |
| 📥 取り込み | CSVからまとめてインポートする |
| 🔍 リストアップ | 手動でリストアップを実行する |
| 🤝 商談記録 | 商談内容を記録する |

### リストアップタブの使い方

1. 検索モードを選ぶ（基本は「🤖 自動モード」）
2. 目標件数を入力（デフォルト50件）
3. 検索期間を選ぶ（基本は「3カ月以内」）
4. 「🚀 リストアップ開始」ボタンを押す
5. 画面下にログが流れ、完了まで待つ（タブを閉じないこと）

---

## B. 専用PCのセットアップ（デーモン担当者のみ）

### ① Python の確認

コマンドプロンプトを開いて実行：

```
python --version
```

`Python 3.11.x` 以上が表示されればOK。
表示されない場合は https://www.python.org/downloads/ からインストール（「Add Python to PATH」にチェック）。

### ② フォルダを配置

```
C:\Users\（自分のユーザー名）\list_tool\
```

に `list_tool` フォルダを置く。

### ③ ライブラリのインストール（初回のみ）

```
cd C:\Users\（自分のユーザー名）\list_tool
pip install -r requirements.txt
```

### ④ .env ファイルの確認

`list_tool` フォルダ内に `.env` ファイルがあること。
中身に HubSpot トークンが設定されていること（担当者から受け取る）。

### ⑤ 動作確認

```
python main.py --list-daemon
```

以下のような表示が出ればOK：

```
📋 リストアップ専用デーモン起動 (サイクル間隔: 60分)
  高スコア＋必須フィールド揃い → 自動登録
  中間スコア → pending_review.json に蓄積（後で python main.py で確認）
```

`Ctrl + C` で停止できる。

### ⑥ PC起動時に自動スタートする設定

**手順：**

1. `Windows + R` キーを押す
2. `shell:startup` と入力して Enter
3. スタートアップフォルダが開く
4. `C:\Users\（自分のユーザー名）\list_tool\start_daemon.bat` を右クリック
5. 「ショートカットの作成」を選ぶ
6. 作成されたショートカットをスタートアップフォルダに移動する

次回ログイン時から自動でデーモンが起動する。

---

## C. デーモン稼働中の日常フロー

### 候補リストの確認・HubSpot登録

デーモンは中間スコアの企業を `pending_review.json` に自動で蓄積し続けます。
定期的に以下を実行して確認・登録します：

```
python main.py
```

起動後の操作：

```
登録方式: 1（確認後に登録）
検索モード: 1（自動モード）または 2（手動でキーワード入力）
```

候補一覧が表示されたら：

| 操作 | 内容 |
|---|---|
| `y` | 全件 HubSpot に登録 |
| `n` | 全件スキップ |
| `1,3` | 番号指定で登録（例: 1番と3番のみ） |
| `x2` | 2番を除外リストに追加 |

### よく使う除外理由コード

| コード | 意味 |
|---|---|
| `m` | メディア・ポータルサイト |
| `s` | 規模が大きすぎる（200名超） |
| `t` | 規模が小さすぎる（10名未満） |
| `i` | 業種NG |
| `o` | その他 |

---

## D. コマンド早見表

```bash
# 通常起動（候補確認・HubSpot登録）
python main.py

# リストアップ専用デーモン（常時稼働）
python main.py --list-daemon

# サイクル間隔を変更する場合（例: 30分ごと）
python main.py --list-daemon --interval=30

# 全自動デーモン（確認なし・全件登録）
python main.py --daemon

# HubSpot登録済み企業の事後チェック
python main.py --audit
```

---

## E. よくあるトラブル

| 症状 | 対処 |
|---|---|
| デーモンが起動しない | Python がインストールされているか確認。`python --version` で確認 |
| HubSpotに登録されない | `.env` のトークンを確認 |
| Webツールが開かない | しばらく待つ（Renderは無操作で一時停止する場合がある） |
| 候補が増えない | デーモンが動いているか確認。コマンドプロンプトに進捗が出ているはず |
