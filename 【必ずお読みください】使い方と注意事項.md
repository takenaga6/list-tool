# リストアップツール 使い方・注意事項

---

## これは何？

Google検索・媒体サイトから企業を自動収集し、HubSpotに登録するツールです。
バックグラウンドで動かし続けることで、営業リストを自動で積み上げます。

---

## フォルダ構成

```
list_tool/
├── 【必ずお読みください】使い方と注意事項.md  ← このファイル
├── setup_token.bat       ← 初回のみ実行（HubSpotトークン設定）
├── start_daemon.bat      ← デーモン起動（毎回ダブルクリック）
├── review_pending.bat    ← 確認画面を開く（ブラウザが開きます）
├── requirements.txt      ← Pythonライブラリ一覧
├── main.py / config.py / app.py / agents/  ← ツール本体（触らない）
└── output/               ← 収集結果が蓄積されるフォルダ
```

---

## 初回セットアップ（一人1回だけ）

### 手順1: Pythonのインストール

Pythonがインストールされていない場合は下記からインストールしてください。
https://www.python.org/downloads/

インストール時に **「Add Python to PATH」に必ずチェック** を入れてください。

### 手順2: 必要ライブラリのインストール

コマンドプロンプトを開き、以下を実行してください。

```
cd （このフォルダのパス）
pip install -r requirements.txt
```

### 手順3: HubSpotトークンの設定

**`setup_token.bat` をダブルクリック**して、表示に従いトークンを入力してください。

トークンは担当者から直接受け取ってください（このフォルダには保存しないこと）。

> ⚠️ トークンは絶対に他人に見せないでください。HubSpotへの不正アクセスの原因になります。

---

## 日常の使い方

### デーモンを起動する

**`start_daemon.bat` をダブルクリック**するだけです。

- 起動後は黒い画面（コマンドプロンプト）が開き、自動で動き続けます
- PCの電源を入れているあいだ、バックグラウンドで企業を収集し続けます
- 終了するときは黒い画面を閉じるか、`Ctrl+C` を押してください

> ⚠️ 同じPCで2人が同時に起動しないでください（データが重複します）

### 確認待ちリストを見る・承認する

**`review_pending.bat` をダブルクリック**するか、下記URLをブラウザで開いてください。

👉 https://list-tool.onrender.com/

1. 「⏳ 確認待ち」タブを開く
2. 登録したい企業の「承認」チェックボックスをオンにする
3. 「承認した企業を保存」ボタンを押す

---

## 収集結果の確認

`output/` フォルダに以下のファイルが蓄積されます。

| ファイル名 | 内容 |
|-----------|------|
| `results.csv` | HubSpot登録済み企業の一覧 |
| `approved_companies.csv` | 確認画面で承認した企業の一覧 |
| `ng_list.csv` | 除外判定された企業の一覧 |
| `tool.log` | 実行ログ（エラー調査用） |

---

## 注意事項まとめ

| # | 注意事項 |
|---|---------|
| 1 | **HubSpotトークンは絶対にこのフォルダに保存しない**（setup_token.batで設定するだけ） |
| 2 | **同時に複数人が起動しない**（データ競合の原因になる） |
| 3 | `main.py` など本体ファイルは**触らない** |
| 4 | `output/` フォルダのCSVは**削除しない**（ツールが重複チェックに使っている） |
| 5 | 黒い画面が出ていれば動いている。途中で閉じても**データは失われない** |

---

## 困ったときは

- 黒い画面がすぐ閉じてしまう → `tool.log` を確認してエラー内容を担当者に共有
- 「確認待ち」タブに何も表示されない → デーモンがまだ起動されていない可能性あり
- トークンエラーが出る → `setup_token.bat` を再実行してトークンを入れ直す
