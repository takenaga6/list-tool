# 架電済みリスト機能 仕様書

## 概要

架電済みリスト・自動リスト補充・A/B/C均等配分の3機能をまとめた仕様です。

---

## 1. 架電済みリストタブ

### 目的
次回アプローチ日が過ぎた企業を一覧表示し、すぐに架電記録を入力できるタブです。

### 表示条件
- `次回アプローチ日` が入力済み **かつ** 今日以前の企業
- 日程が近い順（昇順）に並べて表示

### 主な操作
| 操作 | 説明 |
|---|---|
| 担当者名で絞り込み | ドロップダウンで架電担当者名を選択して絞り込み |
| 会社を選択 | 対象会社を選んでフォームを表示 |
| 架電担当者名の変更 | テキスト入力で自分の名前に変更できる |
| アプローチ内容（架電結果）の入力 | 受付NG / 社長NG / 追客 など選択肢から選ぶ |
| 見込み・備考・次回アプローチ日の入力 | 任意入力 |
| 「記録を更新する」ボタン | 入力内容を保存。保存後にリスト自動補充チェックが走る |

---

## 2. リスト自動補充（HubSpot連動）

### 目的
架電担当者ごとの「まだ架電していない件数（アプローチ内容がブランクの件数）」が少なくなったら、自動でHubSpotから新しい企業を補充します。

### 発動条件
- 架電記録を保存したとき
- 保存した架電担当者名の **アプローチ内容ブランク件数が50件未満** になったら発動

### 補充のルール
| 項目 | 内容 |
|---|---|
| 補充件数 | 100件 |
| 取得元 | 設定で指定したHubSpotリスト（未設定なら全企業） |
| ランク配分 | A / B / C 各33件を優先取得（不足時は他ランクで補完） |
| 担当者名 | 発動した架電担当者名を自動セット |
| アプローチ内容 | ブランク（未架電状態で追加） |

### 補充元リストの設定方法
1. **システム診断タブ** を開く
2. 下部の「**自動補充リスト設定**」セクションを開く
3. ドロップダウンから使用したいHubSpotリストを選択
4. 「**設定を保存**」ボタンを押す

| 選択肢 | 動作 |
|---|---|
| 全企業（指定なし） | HubSpotに登録された全企業から補充（デフォルト） |
| 特定リスト名 | そのリストに入っている企業のみから補充 |

### ランク判定のしくみ
- `results.csv`（リストアップパイプラインで生成）の「ランク」列を参照
- HubSpotにあるがresults.csvにない会社はランク不明として扱い、A/B/Cが不足した枠を補完するために使用

### 自動補充が動くタイミング（3か所）
| 場所 | タイミング |
|---|---|
| 架電済みリストタブ | 「記録を更新する」ボタン押下後 |
| 架電先リスト → インライン架電記録 | 「架電を記録する」ボタン押下後 |
| 架電先リスト → リスト表示 | 「HubSpotに保存」ボタン押下後 |

### 通知
- 補充が実行された場合、画面に `「〇〇」のリスト残数が少ないため XX件を自動補充しました（A/B/C均等）` と表示されます
- 補充不要（50件以上ある）な場合は何も表示しません

---

## 3. 前提・注意事項

- 自動補充には環境変数 `HUBSPOT_TOKEN` の設定が必要です
- `HUBSPOT_TOKEN` が未設定の場合、自動補充はスキップされます（エラーにはなりません）
- 補充はHubSpot APIに対してHTTPリクエストを行うため、保存後に数秒かかる場合があります
- 同じ会社は重複して追加されません（会社名で照合）

---

## データの流れ

```
架電記録を保存
    ↓
担当者のアプローチ内容ブランク件数をカウント
    ↓
50件未満？
  ├─ No → 何もしない
  └─ Yes → HubSpotから300件取得
                ↓
            results.csvでランクを照合
                ↓
            A/B/C各33件を選択（100件）
                ↓
            call_list.csvに追記（担当者名セット済み）
                ↓
            画面に補充件数を通知
```
