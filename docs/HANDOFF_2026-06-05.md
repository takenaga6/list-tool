# 引き継ぎ書 — list-tool クエリ淘汰問題（2026-06-05 着手用）

作成: 2026-06-04 夜 / 対象: 翌日以降の作業者（人 or Claude）
真実の源は `docs/PHASE_HISTORY.md`（特に 2026-06-04 エントリ）。本書は「明日の具体アクション」に絞る。

---

## 0. TL;DR（30秒で把握）

- **問題**：本番でクエリ2,630/2,970件（89%）が淘汰フィルタで死亡し、リストアップの幅が壊滅していた。
- **本日やったこと（全て本番反映済み, Live=`d3a1b5f` 2026-06-04 23:06）**：
  1. 主犯「判定3（A/B無→永久死）」を廃止＋30日復活機構を追加（`df12e88`）。生クエリ 340→2,781 に回復。
  2. 派生バグ（source不一致）を修正（ブランチ `fix/source-attribution`、**未マージ**）。
  3. 5/7起点の謎を解明（リセットではなく永続化開始点。対応不要）。
  4. 勝ち種分析ツール追加（`scripts/analyze_query_patterns.py`）。
- **いま観察中**：`df12e88` の走行で **HubSpot新規登録が回復するか**（1〜2日）。
- **明日の最優先**：観察結果の確認 → OKなら下記②③に進む。

---

## 1. 現在の状態

### 本番（Render, Live=`d3a1b5f`）
- `df12e88`：判定3廃止＋復活機構（**本丸・挙動変更あり**）
- `a87a14d` / `fad21ec`：ドキュメント
- `d3a1b5f`：分析スクリプト（読み取り専用・挙動変更なし）
- `/data` は永続ディスク1GB（確認済み）

### ブランチ
- `main`：上記がpush済み
- `fix/source-attribution`（`9e95e00`, origin退避済み・**未マージ**）：source不一致バグ修正。
  観察と切り分けるため保留中。挙動は診断レポートのみ変わる（パイプライン本体は不変）。

### 重要ファイル
- `agents/keyword_agent.py`：`is_dead_query`（判定1+2のみ＋復活）, `RECOVERY_DAYS=30`, `get_producing_queries`
- `main.py`：record_hit/ng/rank の source 一致（fix/source-attribution ブランチ側）
- `scripts/verify_death_filter.py`：淘汰の新旧比較（実データ検証用）
- `scripts/analyze_query_patterns.py`：勝ち種のテンプレ/媒体/県別集計

---

## 2. 【最優先】走行観察の確認（明日まずこれ）

`df12e88` Live 後、登録ペースが回復したかを見る。

### 見るもの
- HubSpot の新規登録数（df12e88 Live 以降）。判定3で死んでた2,441クエリが回り始めるので増えるはず。
- Render Shell で本番統計を再確認：
  ```bash
  # 生きてるクエリ数が増えているか（旧340 → 新2,781想定）
  python3 scripts/verify_death_filter.py
  # A/B生産の最新状況
  python3 scripts/analyze_query_patterns.py
  ```

### 判定
- **登録が回復 → 止血成功**。②③へ進む。
- **登録が増えず検索回数だけ膨らむ** → 復活クエリが無駄。判定2閾値 or RECOVERY_DAYS を調整。
- **致命的問題** → 即 revert：
  ```bash
  git revert df12e88   # 必要なら。push→Render Live確認まで実施
  ```

---

## 3. 観察OKだったら：作業順序（レバレッジ順）

> CLAUDE.md のコア判定ロジック変更ルール厳守：実装→ユニットテスト→実データ検証→マージ→push→**Render Live目視確認**→観察。
> main直pushしない（ブランチ作業）。`git add`前に `git status` と `git check-ignore`。

### ② 死に媒体35家系を刈る（中レバー・別PR）
- **根拠（本番実データ）**：A/Bゼロ・NG100超の媒体が35家系。各57クエリ×複数期間を空回しして検索予算を浪費。
  例：社長名鑑(133NG/0)、企業と経営(158NG/0)、文芸社(116NG/0)、SUPER CEO(102NG/0)等。
- **やること**：`agents/keyword_agent.py` の `PR_MEDIA` から低A率媒体を外す or 末尾固定。
  空いた枠を productive 家系に回す。
- **残す媒体（A/B実績あり）**：2026年健康経営認定、時代のニューウェーブ、先見経済、
  サンデー毎日「会社の流儀」、エコノミスト ビジネスクロニクル、Leaders AWARD、異業種ネット、KENJA GLOBAL。
- **注意**：媒体単位なら標本100超で「0当たり=実力」と言える（クエリ単位の1%ノイズ問題は回避できる）。
  実データ検証は `analyze_query_patterns.py` の本番出力で前後比較。
- **同時に** `fix/source-attribution` をマージしてよい（媒体別集計が正しくなり、以後の媒体刈り判断が楽になる）。

### ③ 経産省XLSX（健康経営優良法人21,577社）→ list-tool に流す（最大レバー・本命・重い）
- **根拠**：list_page経由のA/B率は **29%**（検索の最良家系の6〜7倍）。健康経営優良法人リストは
  「最初から健康経営の中小企業」なので規模・業種で弾かれにくい。検索チューニングの上限(1〜5%)を超える唯一の道。
- **やること**：別リポジトリ `xlsx-importer`（MVP完成・フォールバック未実装、PHASE_HISTORY参照）の続き。
  gBizINFOでURL+従業員数取得 → 従業員数で事前フィルタ → CSV → list-tool に投入。
- **これは「list-toolのクエリをいじる」話ではなく「良質なリストを供給する」話**。

### やらないこと
- **県別の優先順位付け**：本番データ上ノイズ（総A/B63件を47県に撒いた誤差。県に本質差なし）。

---

## 4. 本番データの要点（勝ち種・2026-06-04時点）

### 家系別 A/B率
| 家系 | A/B | NG | A/B率 |
|---|---|---|---|
| list_page | 5 | 12 | **29%** |
| 業種×健康経営優良法人×県 | 10 | 191 | 5.0% |
| 県×健康経営/福利厚生 | 17 | 406 | 4.0% |
| 2026年健康経営認定×県 | 7 | 167 | 4.0% |
| S2アクサ | 3 | 103 | 2.8% |
| S2大同生命 | 1 | 108 | 0.9% |
| 死に媒体35家系 | 0 | 各80〜160 | 0% |

- 累計：A=20, B=43（計63）, NG=4,747, 検索ヒット総数81,227。検索のA/B率は約1%。
- 具体的勝ちクエリ：アクサ生命 健康経営アクサ式 / 2026年健康経営認定 滋賀県・徳島県・神奈川県 /
  兵庫県・京都府 健康経営優良法人 / 製造業 健康経営優良法人 神奈川県・石川県・長崎県 / 時代のニューウェーブ 鳥取県・静岡県。

---

## 5. 残課題・メモ
- `fix/source-attribution` 未マージ（観察後）。
- 経産省XLSX（③）が本命。xlsx-importer リポジトリで作業。
- keyword_stats は git 管理外（ディスクのみ）。ローカル `output/keyword_stats.json` は5/15の別系統残骸（本番と別物）。
- 観察期間の起点は `df12e88` Live 時刻（`d3a1b5f` は挙動不変なので起点リセットなし）。

---

## 6. コマンド早見表
```bash
# 本番統計の取得・分析（Render Shell）
python3 scripts/verify_death_filter.py        # 淘汰の新旧比較・生クエリ数
python3 scripts/analyze_query_patterns.py     # 勝ち種（テンプレ/媒体/県）

# ローカルテスト（リポジトリルートから）
python -m pytest tests/ -q

# ブランチ確認
git branch -vv
```
